# TODO: Not used for now, but import in .gitconfig with [source] command 
[delta]
	navigate = true
	side-by-side = true
  line-numbers = true
  true-color = always
[core]
	pager = delta
[interactive]
	diffFilter = delta --color-only
