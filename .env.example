# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.

# Space-separated list of work machine hostnames.
# Run `hostname` on each work machine to get its value.
WORK_HOSTNAMES="<TEMP>"
